export default interface User {
  lId: number;
  userName: string;
  userFullName: string;
  IsSelected: boolean;
  cMsgFlag: string;
  isUserLive: boolean;
  mCount: number;
  message: string;
  status: boolean;
  userCode: string;
}
